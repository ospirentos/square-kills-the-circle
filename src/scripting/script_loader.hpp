#pragma once
#include "../entity/entity.hpp"
#include <string>

// [Ne yapıyor] scripts/entities/*.lua dosyalarını tarar ve entity tanımlarını yükler.
namespace ScriptLoader {
    // void LoadAll(const std::string& scriptsDir);
}
