#pragma once
// sol2 header-only library — CMake ile FetchContent üzerinden gelir
// #include <sol/sol.hpp>
#include "../entity/entity.hpp"

// [Ne yapıyor] Lua dosyasından entity tanımını okuyup Entity struct'ına doldurur.
// [Aşama] 4 — sol2 kurulumundan sonra aktif edilecek
namespace LuaBridge {
    // Entity loadFromLua(const char* scriptPath);
}
